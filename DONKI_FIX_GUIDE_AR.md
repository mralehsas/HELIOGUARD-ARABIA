# HELIOGUARD ARABIA v1.0 — NASA DONKI FIX

## التشغيل الموصى به
اضغط مرتين على `START_HELIOGUARD.bat`. ستفتح نافذة سوداء صغيرة ثم يفتح البرنامج على `http://127.0.0.1:8080`. اترك النافذة مفتوحة أثناء الاستخدام.

## لماذا هذا الإصلاح؟
- DEMO_KEY محدود جدًا ويُستهلك بسرعة عند طلب FLR/CME/GST دوريًا.
- بعض المتصفحات تمنع طلبات DONKI من صفحات `file://` بسبب CORS.
- النسخة الجديدة تستخدم كاش 15 دقيقة، تمنع تكرار الطلبات المتزامنة، وتحوّل تلقائيًا بين البوابة المحلية وNASA Open API وخدمة NASA CCMC المباشرة.

## التحقق
افتح مركز الاتصالات، اختر `AUTO` لمسار DONKI، ثم اضغط `فحص جميع الاتصالات`. يجب أن يظهر المصدر باسم `LOCAL_GATEWAY` أو `NASA_CCMC_DIRECT` أو `NASA_OPEN_API`.

## مفتاح NASA
المفتاح الشخصي اختياري في وضع AUTO لأن CCMC المباشر لا يحتاج مفتاحًا. وجود المفتاح يحسن الوصول إلى NASA Open API.
