# HELIOGUARD ARABIA v1.0.1 — OFFICIAL UI HOTFIX

## العربية
- إصلاح خطأ JavaScript في ربط زر مركز الاتصالات (`$$` بدل `$`) كان يوقف تنفيذ بقية مستمعات الأزرار.
- إصلاح زر التبديل عربي/إنجليزي.
- تهيئة اللغة المحفوظة عند بدء التشغيل وضبط `lang` و`dir` واتجاه الواجهة.
- تشغيل مراقب الترجمة للمحتوى الديناميكي.
- إصلاح زر «حول | About» وإظهار القسم العربي أو الإنجليزي بحسب اللغة الحالية.
- اختبار الفتح والإغلاق والتبديل بين اللغتين داخل متصفح Chromium فعلي.

## English
- Fixed a JavaScript selector typo that stopped registration of the remaining button handlers.
- Restored AR/EN language switching, persisted language initialization, and layout direction.
- Enabled dynamic-content translation observation.
- Restored the About modal and its language-specific content.
- Verified both controls in a real Chromium browser test.
